<%= form_with model: @post_comments, url: item_post_comments_path, local: false do |f| %>
    <div class= "form-group">
      <%= f.text_area :comment, placeholder: "コメントをここに", class: "form-control introduction"%>
      <%= f.submit "送信する" %>
    </div>
<% end %>