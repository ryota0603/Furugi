<section class="jumbotron text-center">
    <div class="container">
      <!-- <h1 class="jumbotron-heading">Album example</h1> -->
      <h2 class="jumbotron-heading">投稿一覧<span class="small">(全 <%= @items.count %> 件)</span></h2>
      <!-- <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p> -->
      <p class="lead text-muted">内容や作成者など, 以下のコレクションに関する簡単な説明文を書きましょう。なるべく短く, わかりやすい文にしましょう。しかし短すぎると, 友達は見てくれないかもしれません。</p>
    </div>
</section>
<div class="album py-5 bg-light">
    <div class="container">
      <div class="row">
        <div class="col-md-3">
          <div class="card mb-4 shadow-sm">
          <% @items.each do |item| %>
           <%= link_to item_path(item.id) do %>
            
              <% if item.image.present? %>
               <%= image_tag item.image, size: '300x150' %>
              <% end %>
              <div class="card-body">
                <!-- <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p> -->
                <p class="text-dark"><%=item.name %></p>
                <p class="text-dark"><%= item.body %></p>
                <p>いいね数：<%= item.favorites.count %></p>
                <p>コメント件数：<%= item.post_comments.count %></p>
              </div>
           <% end %>
          <% end %>
          </div>
        </div>
      </div>
    </div>
</div>
  <div class="mt-4 d-flex justify-content-center">
     <%= paginate @items %>
  </div>
  

 
 
    　　


